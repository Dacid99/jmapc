build_docker:
  stage: build
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - echo "Building and publishing python package ..."
    - poetry publish --build
    - echo "Successfully released python package."
